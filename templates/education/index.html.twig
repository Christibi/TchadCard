{% extends 'base.html.twig' %}

{% block body %}
	{% for msg in app.flashes("success") %}

		<div class="alert alert-success">{{ msg }}</div>
	{% endfor %}
	<div class="pagetitle">
		<h1>Education</h1>
		<a href="{{ path("app_importer") }}">
			<span>Importer</span>
		</a>
	</div>
	<!-- End Page Title -->
	<section class="section">
		<div class="row">
			<div class="col-lg-12">

				<div class="card">
					<div class="card-body">
						<!-- Table with stripped rows -->
						<table class="t2" border = 1>
							<thead>
                                <tr>
                                    <th scope="col">N°</th>
                                    <th scope="col">Indicateur</th>
                                    <th scope="col">Province</th>
									<th scope="col">Niveau</th>
                                    <th scope="col">Nombre de Fille</th>
                                    <th scope="col">Nombre de Gacon</th>
                                    <th scope="col">Annees</th>
                                    <th scope="col">ACTIONS</th>
                                </tr>
							</thead>
							<tbody>
								{% for Education in MesEducations %}
									<tr>
										<th>
                                            {{ loop.index}}
										<td>
											{{Education.indicateur}}
										</td>
										<td>
											{{Education.province}}
										</td>
										<td>
											{{Education.niveau}}
										</td>
										<td>
											{{Education.Nbrefe}}
										</td>
										<td>
											{{Education.Nbreh}}
										</td>
										<td>
											{{Education.annee}}
										</td>

										<td>
											<a href="{{ path("app_supprimer_education",{id:Education.id}) }}">del</a>
											<a href="{{ path("app_editer_education",{id:Education.id}) }}">edit</a>
										</td>
									</tr>

								{% endfor %}
							</tbody>
						</table>
						<!-- End Table with stripped rows -->


					</div>
				</div>

			</div>
		</div>
	</section>
{% endblock %}

